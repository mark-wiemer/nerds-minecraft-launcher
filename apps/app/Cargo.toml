[package]
name = "theseus_gui"
version = "0.9.0"
description = "Nerd's Minecraft Launcher (NML) is a desktop application for managing your Minecraft mods. Fork of Modrinth App, not associated with Rinth, Inc."
license = "GPL-3.0-only"
repository = "https://github.com/mark-wiemer/nerds-minecraft-launcher"
edition = "2021"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2.0.3", features = ["codegen"] }

[dependencies]
theseus = { path = "../../packages/app-lib", features = ["tauri"] }

serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_with = "3.0.0"

tauri = { version = "2.1.1",  features = ["devtools", "protocol-asset", "unstable"] }
tauri-plugin-window-state = "2.2.0"
tauri-plugin-deep-link = "2.2.0"
tauri-plugin-os = "2.2.0"
tauri-plugin-opener = "2.2.1"
tauri-plugin-dialog = "2.2.0"
tauri-plugin-updater = { version = "2.3.0" }
tauri-plugin-single-instance = { version = "2.2.0" }

tokio = { version = "1", features = ["full"] }
thiserror = "1.0"
futures = "0.3"
daedalus = { path = "../../packages/daedalus" }
chrono = "0.4.26"

dirs = "5.0.1"

url = "2.2"
uuid = { version = "1.1", features = ["serde", "v4"] }
os_info = "3.7.0"

tracing = "0.1.37"
tracing-error = "0.2.0"

lazy_static = "1"
once_cell = "1"

dashmap = "6.0.1"
paste = "1.0.15"

opener = { version = "0.7.2", features = ["reveal", "dbus-vendored"] }

native-dialog = "0.7.0"

[target.'cfg(target_os = "linux")'.dependencies]
tauri-plugin-updater = { version = "2.3.0", optional = true, features = ["native-tls-vendored", "zip"], default-features = false }

[features]
# by default Tauri runs in production mode
# when `tauri dev` runs it is executed with `cargo run --no-default-features` if `devPath` is an URL
default = ["custom-protocol"]
# this feature is used for production builds where `devPath` points to the filesystem
# DO NOT remove this
custom-protocol = ["tauri/custom-protocol"]
updater = []
