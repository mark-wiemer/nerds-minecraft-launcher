[package]
name = "daedalus"
version = "0.2.3"
authors = ["Jai A <jai@modrinth.com>", "Mark Wiemer (NML fork)"]
edition = "2021"
license = "MIT"
description = "Utilities for querying and parsing Minecraft metadata. Fork of Modrinth Daedalus, not associated with Rinth, Inc."
repository = "https://github.com/mark-wiemer/nerds-minecraft-launcher"
include = ["Cargo.toml", "src/**/*.rs", "README.md", "LICENSE"]
keywords = ["minecraft", "launcher"]
categories = ["game-development", "api-bindings"]
readme = "README.md"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
bytes = "1"
thiserror = "1.0"
